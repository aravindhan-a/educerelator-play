# Set the real values in the Vercel dashboard (Project Settings > Environment Variables).
# Never commit a filled-in .env to git.

ANTHROPIC_API_KEY=
# KV_REST_API_URL and KV_REST_API_TOKEN are added automatically when you
# attach a Vercel KV store to this project -- no need to set manually.
